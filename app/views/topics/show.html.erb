<h1><%= @topic.title %></h1>

<div class="row">
  <div class="col-md-8">
    <% @topic.bookmarks.each do |b| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= b.url %> 
          </h4>
        </div>
      </div>
    <% end %>
  </div>
